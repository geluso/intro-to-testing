var assert = require('assert');
var maxWeatherSpike = require('../max-weather-spike');

describe("JavaScript Mathematics", function() {
  it("should add numbers correctly", function() {
    assert.equal(1 + 1, 2);
  });
});
